# API for touchstone package

## Exported functions

benchmark_ls(name)
benchmark_read(name, ref)
benchmark_run_ref(expr_before_benchmark, ..., refs = c(Sys.getenv("GITHUB_BASE_REF", abort_missing_ref()), Sys.getenv("GITHUB_HEAD_REF", abort_missing_ref())), n = 100, path_pkg = ".")
benchmark_write(benchmark, name, ref, block = NA, iteration = NA, append = TRUE)
benchmarks_analyze(refs = c(Sys.getenv("GITHUB_BASE_REF", abort_missing_ref()), Sys.getenv("GITHUB_HEAD_REF", abort_missing_ref())))
dir_touchstone()
refs_install(refs = c(Sys.getenv("GITHUB_BASE_REF", abort_missing_ref()), Sys.getenv("GITHUB_HEAD_REF", abort_missing_ref())), path_pkg = ".", install_dependencies = FALSE)
touchstone_clear(all = FALSE)
use_touchstone(cancel = TRUE)
with_touchstone_lib(path, ref = Sys.getenv("GITHUB_HEAD_REF", abort_missing_ref()))
